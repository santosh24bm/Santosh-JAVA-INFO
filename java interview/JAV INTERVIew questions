spring reposotories 



rest : 

can xml is used with same method or need the separate method - same mehtod is used for differnt request types ?

if there are so many parameters in request which request methos is good ? 


how hibernate second level object can be deleted ? 

fail safe and fail fast ? 

how to select branches in github ? 

how to revert to old version after changing the code in git hub

reflections uses 

difference bewtween stream and collection 
 parallel array vs parallel collection 
 
 ======================================
 
 java 7 features :
 
 1 . functional programming is enabled 
2. boiler code is removed 
3. allows parallel processing 


why ?

when is the funcional programmingis useful ?
 
